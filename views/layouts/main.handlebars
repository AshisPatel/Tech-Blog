<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="/stylesheets/style.css" rel="stylesheet" />
</head>

<body class="bg-butter">
    {{!-- Main page will always display a navbar at the top and posts in the middle --}}
    <header>
        <nav class="d-flex flex-row justify-content-between p-5 mb-5 bg-chocolate">
            <div class="d-flex flex-row p-0 mr-2">
                <h1 class="text-cream">
                    {{!-- homepage text is a link that links back to homepage --}}
                    <a href="/">TECH BLOG</a>
                </h1>
            </div>
            {{!-- Navbar buttons --}}
            <div class="d-flex flex-row">
                {{#if signedIn}}
                <button type="button" name="dashboard-btn" class="text-chocolate me-3 gradient-btn">Dashboard</button>
                <button type="button" name="sign-out-btn" class="gradient-btn text-chocolate me-3">Sign-out</button>
                {{else}}
                <button type="button" name="sign-in-btn" class="gradient-btn text-chocolate me-3">Sign-in</button>
                <button type="button" name="sign-up-btn" class="gradient-btn text-chocolate me-3">Sign-up</button>
                {{/if}}
            </div>
        </nav>
    </header>
    <main>
        {{{body}}}
        
        {{#if signedIn}}
        <script src="/javascript/signedInBtns.js"></script>
        {{else}}
        <script src="/javascript/signedOutBtns.js"></script>
        {{/if}}
    </main>
    <footer>
        Made with ☕ & 💖
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-/bQdsTh/da6pkI1MST/rWKFNjaCP5gBSY4sEBT38Q/9RBh9AH40zEOg7Hlq2THRZ" crossorigin="anonymous"></script>
</body>



</html>